var config = require('./../../config/config');
/**
 * Объект конфига
 */
var mainConfig = {
  "localization": {
    "english": "en",
    "german": "de"
  },
  main_config: config.app.public_config
};

module.exports = mainConfig;